{
  "name": "@code-world/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun dist/index.js",
    "lint": "biome check .",
    "test": "vitest run"
  },
  "dependencies": {
    "@code-world/db": "workspace:*",
    "@code-world/types": "workspace:*",
    "@hono/zod-validator": "^0.4.2",
    "better-auth": "^1.2.0",
    "hono": "^4.6.20",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@code-world/config": "workspace:*",
    "@types/bun": "^1.1.14",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
