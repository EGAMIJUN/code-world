{
  "name": "@code-world/executor",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun dist/index.js",
    "lint": "biome check .",
    "test": "vitest run"
  },
  "dependencies": {
    "@code-world/db": "workspace:*",
    "@code-world/types": "workspace:*",
    "bullmq": "^5.34.6",
    "drizzle-orm": "^0.45.2",
    "ioredis": "^5.4.2",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@code-world/config": "workspace:*",
    "@types/bun": "^1.1.14",
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3",
    "vitest": "^4.1.8"
  }
}
